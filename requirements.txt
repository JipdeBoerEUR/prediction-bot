# Install everything with one command:
#   pip install -r requirements.txt
#
# For CPU-only PyTorch (smaller download, no CUDA):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# --- HTTP & Web Scraping ------------------------------------------------------
aiohttp>=3.9.0
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2

# --- Data & Finance -----------------------------------------------------------
yfinance==1.2.0
pandas==3.0.1
numpy==2.4.3
scipy==1.17.1

# --- Machine Learning ---------------------------------------------------------
scikit-learn==1.8.0
joblib==1.5.3
torch==2.10.0
transformers==5.3.0
sentence-transformers==5.3.0

# --- Vector DB & RAG ----------------------------------------------------------
chromadb==1.5.5

# --- Google GenAI (Gemini / RAG) ----------------------------------------------
google-genai==1.68.0

# --- Scheduling ---------------------------------------------------------------
APScheduler>=3.10.0

# --- Charting & Graph Analysis ------------------------------------------------
matplotlib==3.10.8
networkx==3.6.1

# --- Trading Execution (Alpaca) -----------------------------------------------
alpaca-py==0.43.2
# NOTE: the legacy root-level execution.py optionally imports alpaca-trade-api,
# but that file is retired (superseded by statarb/execution.ExecutionEngine,
# see main.py) and its import is already wrapped in try/except ImportError.
# alpaca-trade-api 3.2.0 pins websockets<11, which conflicts with yfinance /
# google-genai / alpaca-py's websockets>=13 requirement — so it is deliberately
# NOT listed here. Uncomment only if you still use the legacy execution.py:
# alpaca-trade-api>=3.2.0

# --- SEC Filings (EDGAR) ------------------------------------------------------
edgartools>=2.0

# --- Optimization -------------------------------------------------------------
optuna>=3.6.0

# --- Topical Trading (BERTopic + NLP) -----------------------------------------
feedparser>=6.0.10
bertopic>=0.16.0
umap-learn>=0.5.6
hdbscan>=0.8.33
vaderSentiment>=3.3.2
nltk>=3.8.1

# --- Utilities ----------------------------------------------------------------
python-dotenv==1.2.2

# --- Optional: statarb topological / forex extras -----------------------------
# These are imported under try/except in the statarb engine and are only needed
# if you run the topological data analysis or Dukascopy forex features.
# Uncomment if you use them:
# giotto-tda>=0.6.0
# gudhi>=3.8.0
# dukascopy-python>=4.0.0
